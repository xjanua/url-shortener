<configuration>
    <appender name="console" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>
                %d{yyyy-MM-dd HH:mm:ss} %highlight(%-5level) %logger{36} - %msg%n
            </pattern>
        </encoder>
    </appender>

    <!-- Cấu hình logger cho các class và mức độ log -->
    <logger name="org.springframework" level="INFO" />
    <logger name="com.yourcompany" level="DEBUG" />
    <logger name="com.restaurant" level="ERROR" />

    <root level="INFO">
        <appender-ref ref="console" />
    </root>
</configuration>